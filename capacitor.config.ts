import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.thesociety',
  appName: 'The Society',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
